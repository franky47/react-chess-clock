{
  "name": "react-chess-clock",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "moment": "^2.20.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.0.17"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "surge -d https://chessclock.surge.sh -p ./build",
    "release": "yarn build && yarn deploy"
  },
  "devDependencies": {
    "surge": "^0.19.0"
  },
  "standard": {
    "globals": [
      "test",
      "describe",
      "it"
    ],
    "parser": "babel-eslint"
  }
}
